services:
  api:
    build:
      context: .
    container_name: projeto-back-api
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      VERSION: 1.1.0
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:8080}
    restart: unless-stopped
